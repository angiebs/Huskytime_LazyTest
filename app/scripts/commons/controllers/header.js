define([
    'angular',
    'jquery',
    'commons/module'
  ],
  function (angular,$, ModuleManager) {
    'use strict';

    ModuleManager.controller('HeaderController',
      [
        '$scope',
        '$log',
        '$timeout',
        function ($scope,$log, $timeout) {
          //Your Magic here

        }
      ]
    );
  });
